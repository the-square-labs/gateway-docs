---
title: Good Gateway documentation
description: Operate applications, ingress, Docker, databases, observability, and AI from one infrastructure control plane.
section: Start here
order: 1
audience: [operator, platform-engineer, security-admin, developer]
status: ready
lastReviewed: 2026-08-30
---

# Good Gateway documentation

Good Gateway is an infrastructure control plane for teams that need a coherent way to operate ingress, Docker workloads, managed databases, certificates, observability, access, automation, and AI services. You can use the complete Operations Console, REST API, OAuth, remote MCP tools, or the AI Workspace. AI is an optional operating surface, not a dependency for core infrastructure management.

![Gateway Dashboard with ingress, managed database, and node health](/screenshots/product-tour/dashboard.png)

## Choose your path

- **Evaluating the product:** start with [Product tour](getting-started/product-tour.mdx), [Architecture](concepts/architecture.mdx), and [Capabilities](reference/capabilities.mdx).
- **Installing a new instance:** follow [Requirements](getting-started/requirements.mdx), [Install Gateway](getting-started/install.mdx), and [Initial setup](getting-started/initial-setup.mdx).
- **Publishing an application:** use the complete [Publish an application](journeys/publish-application.mdx) journey.
- **Connecting an application to a private database:** use [Private application database](journeys/private-database.mdx).
- **Operating production:** read [Production readiness checklist](operations/production-checklist.mdx), [Updates and backups](operations/updates-backups.mdx), and [Incident runbook](operations/incident-runbook.mdx).
- **Integrating tools:** use [Scopes, tokens, and OAuth](identity/scopes-tokens-oauth.mdx) and the programmatic-access reference in the repository operations guide.

## The product model in one minute

Gateway runs as a central application with PostgreSQL and Redis. Managed node daemons connect outbound through the Gateway Relay using authenticated transport. Operators define desired state in Gateway; daemons apply bounded operations on their own hosts. Gateway keeps identity, authorization, audit, resource relationships, and recovery state centrally.

The most important resource chains are:

```text
Domain -> TLS certificate -> Ingress Route -> application upstream
Git repository -> Build Worker -> immutable artifact -> Docker workload
Application workload -> managed database binding -> private database identity
Health and runtime events -> notifications -> status pages / SIEM
```

## What this documentation includes

This portal combines four kinds of material:

- **Guides:** goal-oriented instructions for configuring a capability.
- **Journeys:** complete happy paths spanning multiple Gateway areas.
- **Runbooks:** operational diagnosis, recovery, updates, and disaster preparation.
- **Reference:** stable terminology, ports, feature status, plans, and security boundaries.

Roadmap-only capabilities are labeled explicitly and must not be treated as available runtime features.
