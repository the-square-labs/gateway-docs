---
title: Обзор Pages
description: Размещайте неизменяемые релизы статических сайтов с Tags, Routes, previews и конфигурацией runtime.
section: Pages
order: 1
audience: [developer, platform-engineer]
status: ready
lastReviewed: 2026-08-30
---


Pages публикует статические сайты как Projects с неизменяемыми Deployments и изменяемыми Tags. Deployment — фиксированный артефакт, созданный ручной загрузкой или Git-сборкой. Tag — перемещаемый именованный указатель на один Deployment. Routes указывают на Tags, а не напрямую на Deployments, поэтому promotion и откат становятся намеренными эксплуатационными действиями, а не изменениями уже опубликованного артефакта.

## Модель ресурсов и владение

Pages Project владеет историей развёртываний, Tags, Routes, конфигурацией runtime, отношениями доступа и поведением хранения. Gateway централизованно хранит каждый опубликованный артефакт и материализует его на назначенных Nodes nginx. Nginx обслуживает применённую конфигурацию Pages, а Gateway остаётся источником истины для выбора Tag, который должен использовать Route.

Deployments неизменяемы: изменение файлов сайта, результата сборки или входных данных источника создаёт новый Deployment. Tags изменяемы: перемещение Tag меняет уже созданный Deployment, на который он указывает. Системный Tag `latest` отслеживает подходящее текущее развёртывание; используйте пользовательские Tags для именованных окружений, каналов релизов или явного барьера promotion.

## Публикация из Git или вручную

Для релиза из Git поддерживаемая интеграция источника и Build Worker создают новый неизменяемый Deployment. Для ручного релиза передайте готовый артефакт статического сайта и создайте из него новый Deployment. В обоих случаях проверьте итоговый артефакт и идентичность развёртывания до перемещения публичного Tag.

Создание Deployment не публикует его на пользовательском домене. Публикация начинается, когда Route указывает на Tag, который выбирает этот Deployment. Это позволяет команде проверить релиз до promotion, а при инциденте — откатить Tag без изменения предыдущего артефакта.

## Routes, previews и конфигурация runtime

Пользовательские Routes указывают только на Tags. До объявления релиза проверьте Domain, TLS-сертификат, размещение nginx и цель Tag. Необязательные wildcard-previews используют неизменяемые hostname развёртываний; они удобны для проверки конкретного артефакта без изменения публичного Tag.

Конфигурация runtime — публичные клиентские данные, отделённые от идентичности неизменяемого артефакта. Сайт получает её как `window.runtime.config` с объектом `Default` и полными переопределениями объекта для Tags. Неизменяемые previews используют `Default`; Route с Tag использует его переопределение, если оно есть, или возвращается к `Default`. Не помещайте в конфигурацию runtime секреты, учётные данные или приватную топологию.

## Продвиньте, откатите и проверьте

1. Создайте новый неизменяемый Deployment через Git или ручную публикацию.
2. Проверьте результат сборки или загрузки и протестируйте артефакт через подходящий preview или контролируемый Route.
3. Переместите нужный Tag на проверенный Deployment.
4. Проверьте путь DNS и TLS Route, ожидаемый ответ, статические assets и видимую браузеру конфигурацию runtime.
5. После promotion наблюдайте за Route, состоянием приложения nginx и операциями проекта.

Для отката переместите Tag Route на предыдущий заведомо исправный Deployment. Старый артефакт остаётся неизменным, а откат фиксируется как изменение публикации Tag. Сохраняйте обратную совместимость конфигурации runtime, пока Tag может перемещаться между релизами; изменение конфигурации runtime не создаёт новый Deployment.

## Последствия жизненного цикла

Удаление Deployment исключает этот неизменяемый релиз из будущего выбора и может убрать цель отката. Удаляйте его только после проверки требований хранения и ссылок Tags. Удаление Tag или Route меняет доступность, а не содержимое выбранного Deployment. Отключение Pages убирает функцию из обычной навигации, сохраняя данные проекта.

## Следующий шаг

- [Соберите и опубликуйте Deployment Pages из Git](./git-deployments)
