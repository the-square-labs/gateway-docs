---
title: DNS, email и webhooks
description: Настройте внешние системы доставки и проверьте поведение при сбоях.
section: Интеграции и автоматизация
order: 2
audience: [operator, security-admin]
status: ready
lastReviewed: 2026-08-30
---


Эти integrations связывают решения Gateway с системами вне его контроля. Владелец платформы определяет результат, владелец external service выдает least-privilege credentials, а владелец сервиса проверяет delivery со стороны получателя. Успех требует принятия Gateway и независимого подтверждения через DNS resolvers, mail recipients или webhook consumers.

Интеграция Cloudflare может управлять поддерживаемыми записями Domain и проверками DNS-01. Используйте API-токен с узкой областью и проверьте нужные account и zone до включения реконсиляции.

Конфигурация email поддерживает потоки аутентификации и доставку уведомлений там, где они включены. До зависимости от одноразовых кодов по email проверьте идентичность отправителя, TLS, доставляемость и rate limits.

Webhooks могут запускать поддерживаемые процессы Deployment или автоматизации. Используйте сгенерированные secrets, проверяйте подписи, делайте получателей идемпотентными и отличайте принятую операцию от завершённого развёртывания. Не возвращайте внутренние ошибки daemon или учётные данные в ответах webhook.

## DNS-коннекторы

Используйте отдельный токен Cloudflare, ограниченный нужными account, zones и правами DNS. Проверьте allowlist зон до включения управляемых записей или проверок DNS-01. Gateway должен владеть только записями, явно подключёнными к управляемым Domains; несвязанные записи остаются внешним состоянием.

Для миграции зафиксируйте текущие значения и TTL, подтвердите целевой адрес Ingress, примените изменение и проверьте разрешение через независимые публичные resolvers. Не удаляйте старый endpoint, пока клиентский трафик не достигнет нового размещения.

## Email

Настройте идентичность отправителя, SMTP host, port, режим TLS и аутентификацию согласно требованиям provider. Отправьте тестовое сообщение и проверьте реальный inbox, классификацию spam, согласование SPF/DKIM/DMARC и rate limits provider.

Если включены одноразовые коды по email, сохраняйте другой путь восстановления администратора. Успешное SMTP-соединение не доказывает, что пользователи получают сообщения аутентификации.

## Webhooks уведомлений

1. Создайте HTTPS receiver с ограниченным размером запроса и timeout.
2. Настройте аутентификацию или подпись HMAC.
3. Отправьте тестовое событие и проверьте подпись по сырому телу запроса.
4. Возвращайте детерминированный успешный ответ только после надёжного принятия события получателем.
5. Сделайте повторы идемпотентными с помощью идентификатора события или доставки.
6. Отслеживайте историю доставки, задержку, повторы и terminal failures.

## Webhooks развёртывания

Считайте доставку webhook запросом на оценку события источника, а не доказательством развёртывания. Независимо проверьте подпись provider, allowlist репозитория, политику ветки или tag, разрешённый commit, допуск сборки и финальную Task развёртывания.

Ротируйте secrets коннектора и webhook, устанавливая замену, проверяя её и только затем отзывая старое значение. Не раскрывайте полные ответы receiver, учётные данные provider или внутренние ошибки daemon недоверенным вызывающим сторонам.

## Сбои и восстановление

DNS обычно ломается из-за неверного account или zone, недостаточных прав token, кеша resolver, неожиданного proxy/CDN mode или challenge record, который не виден публично. Сравните запись Gateway с ответом authoritative nameserver и хотя бы одного независимого resolver. При миграции восстановите заранее записанное прежнее значение, если новый endpoint нельзя подтвердить до согласованного срока rollback.

У email-доставки несколько независимых этапов: Gateway принял сообщение, SMTP provider принял его, принимающая система приняла его, пользователь может его найти. Проверяйте каждый этап до изменения authentication policy. Если доставка one-time codes деградировала, сохраняйте проверенный альтернативный путь администратора и не создавайте повторные коды до provider throttling.

Для webhook-сбоя нужны delivery record, номер попытки, класс ответа и correlation ID приемника. Ответ `2xx` должен означать, что приемник надежно принял событие; дальнейшая обработка может быть асинхронной. Для retryable failures приемник должен дедуплицировать по delivery или event ID. При terminal authentication failure смените shared secret или signing key, проверьте обе стороны и только затем повторите ограниченный набор неудачных доставок.

## Границы безопасности

Используйте HTTPS endpoints без credentials в URL. Ограничьте исходящие назначения ожидаемыми публичными хостами или явно одобренными private CIDRs. Приемник должен проверять timestamp или replay window, если это поддерживает интеграция, сравнивать signatures в constant time, ограничивать размер запроса и не отражать body в ошибке.

DNS- и email-токены провайдеров остаются административными credentials, даже если Gateway шифрует их at rest. Включите provider-side audit, регулярно проверяйте scopes и отзывайте старый token только после реальной тестовой операции с заменой.

Для business-critical authentication или notification документируйте альтернативный communication и administrator-recovery path. Доступность и latency внешнего provider находятся вне availability boundary Gateway, поэтому один непроверенный provider не должен быть единственным способом войти или управлять installation.
