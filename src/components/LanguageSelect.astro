---
import DefaultLanguageSelect from '@astrojs/starlight/components/LanguageSelect.astro';

// The generated API reference has one unlocalized route tree.
const isApiReference = Astro.url.pathname === '/api' || Astro.url.pathname.startsWith('/api/');
---

{!isApiReference && <DefaultLanguageSelect />}
