interface:
  display_name: "Publish to Gateway Pages"
  short_description: "Publish HTML to Gateway Pages and share a link"
  default_prompt: "Use $publishing-html-pages to publish this HTML output to Gateway Pages and share a verified link."
