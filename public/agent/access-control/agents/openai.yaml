interface:
  display_name: "Gateway Access Control"
  short_description: "Manage Gateway users, groups, scopes, and grants"
  default_prompt: "Use $access-control to review or change this Gateway access with least privilege."
